---
phase: 08-validar-dados-da-tela-web-contra-o-legado
plan: 01
type: execute
wave: 1
depends_on:
  - phase-07
files_modified:
  - src/main/java/br/com/salome/core/application/contaspagar/**
  - src/main/java/br/com/salome/core/domain/contaspagar/**
  - src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/**
  - src/test/java/br/com/salome/core/application/contaspagar/**
  - src/test/java/br/com/salome/core/domain/contaspagar/**
  - docs/setup/salome-core-local.md
  - .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md
  - .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-01-SUMMARY.md
autonomous: true
requirements:
  - READ-03
user_setup:
  - "Use JDK 25 and Maven when running tests or the validation command locally."
  - "Live validation requires SALOME_LEGACY_DB_ENABLED=true plus SALOME_LEGACY_DB_URL, SALOME_LEGACY_DB_USERNAME and SALOME_LEGACY_DB_PASSWORD."
  - "The validator must connect to a safe read-only legacy MySQL environment; do not point it at production without explicit approval from the responsible team."
must_haves:
  truths:
    - "Decision D-01 is covered: validation runs for Hoje, Vencidas, Proximos dias, Todas and manual period."
    - "Decision D-02 is covered: validation compares the main duplicata grid and full selected detail, including products, parcels and rateio."
    - "Decision D-03 is covered: comparison is strict for text, money, dates, status, links, counts, ordering and detail values."
    - "Decision D-04 is covered: expected values are derived from mapped legacy queries and documented Swing behavior when a query alone is ambiguous."
    - "Decision D-05 is covered: live validation reads the complete legacy MySQL result set for the phase recorte, not mocks or artificial samples."
    - "Decision D-06 is covered: every returned record is processed, while the report summarizes totals and representative divergences instead of dumping the entire database."
    - "Decision D-07 is covered: evidence selection records pending, overdue, paid/baixada, with/without products, with/without rateio and with/without extrato when present."
    - "Decision D-08 is covered: divergences list expected legacy/MySQL value, actual salome-core value and the exact field/path."
    - "Decision D-09 is covered: this phase documents divergences and only fixes narrow validation blockers; broad parity fixes are planned separately."
    - "Decision D-10 is covered: the final report is versioned as 08-VALIDATION.md in the phase directory."
    - "Decision D-11 is covered: fields without clear origin are recorded as pending-origin divergences with a technical question."
    - "Decision D-12 is covered: a reusable validation service/runner compares legacy reads against the new adapter/service and writes the Markdown report."
    - "Decision D-13 is covered: validation runs as a local command/procedure outside the Vaadin UI."
    - "Decision D-14 is covered: missing legacy DB configuration fails loudly with a clear list of required variables/configuration."
    - "Decision D-15 is covered: the comparator has automated fixture tests that do not require a live MySQL database."
    - "Vaadin View remains unchanged unless needed for a read-only display bug; no validation button is added."
    - "No SQL or financial rule is added to a Vaadin View."
    - "No write, baixa, edit, delete, mutable rateio or payment command is exposed."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      provides: "Actual read path used by the Vaadin screen"
      contains: "carregarPainel"
    - path: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosRepository.java"
      provides: "Read-only repository port to compare through"
      contains: "listarTitulos"
    - path: "src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java"
      provides: "Current salome-core JDBC adapter under validation"
      contains: "notacompraduplicatas"
    - path: "src/main/java/br/com/salome/core/domain/contaspagar/GestaoPagamentosFiltro.java"
      provides: "Filter model for every validation scenario"
      contains: "PERIODO_MANUAL"
    - path: ".planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md"
      provides: "Versioned validation report with totals, divergences and pending origins"
      contains: "READ-03"
  key_links:
    - from: "local validation runner"
      to: "GestaoPagamentosService"
      via: "actual salome-core snapshot collection"
      pattern: "carregarPainel"
    - from: "local validation runner"
      to: "expected legacy query adapter"
      via: "read-only JdbcTemplate queries based on mapped legacy behavior"
      pattern: "notacompra"
    - from: "comparison engine"
      to: "08-VALIDATION.md"
      via: "Markdown report writer"
      pattern: "expected | actual | path"
---

<objective>
Implement a reusable Phase 8 validation path that confirms the read-only Vaadin `Gestao de Pagamentos` data matches legacy MySQL and mapped Swing behavior before any write phase begins.

Purpose: compare every Phase 8 filter and the full title/detail/product/parcel/rateio read model, record strict divergences in `.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md`, and add fixture tests for the comparator engine without depending on a live database.

Decision coverage: this plan translates D-01 through D-15 from `08-CONTEXT.md` into executable validation work for READ-03.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-CONTEXT.md
@.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-RESEARCH.md
@.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md
@.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-VERIFICATION.md
@.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-01-SUMMARY.md
@docs/architecture/salome-core-architecture.md
@docs/setup/salome-core-local.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation domain model and strict comparator</name>
  <files>src/main/java/br/com/salome/core/domain/contaspagar/**, src/main/java/br/com/salome/core/application/contaspagar/validation/**, src/test/java/br/com/salome/core/application/contaspagar/**</files>
  <read_first>.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-CONTEXT.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-RESEARCH.md, src/main/java/br/com/salome/core/domain/contaspagar/*.java</read_first>
  <action>Create small validation types for scenario/filter, record key, comparison field path, expected value, actual value, origin reference and divergence status. Add a comparator that accepts expected and actual snapshots for titles, selected detail, products, parcels and rateios. Compare all values strictly after normalizing only representation-level concerns that do not change business meaning, such as `BigDecimal.compareTo` for numeric equality and canonical ISO dates. Do not hide differences with fuzzy matching. Include ordering and count comparisons. Fields without a mapped origin should be emitted as `pending-origin` divergences with a technical question rather than skipped.</action>
  <verify>rg -n "Diverg|expected|actual|pending-origin|compareTo|fieldPath" src/main/java/br/com/salome/core src/test/java/br/com/salome/core</verify>
  <acceptance_criteria>
    - Comparator can report missing records, extra records, field mismatches, ordering mismatches and pending-origin fields.
    - Comparator output includes filter/scenario, record key, field/path, expected value, actual value and origin reference.
    - Monetary comparison does not fail only because of `BigDecimal` scale differences.
    - No live MySQL connection is required for comparator tests.
  </acceptance_criteria>
  <done>Validation has a reusable, testable comparison core.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture tests for READ-03 comparison behavior</name>
  <files>src/test/java/br/com/salome/core/application/contaspagar/**, src/test/java/br/com/salome/core/domain/contaspagar/**</files>
  <read_first>src/test/java/br/com/salome/core/application/contaspagar/GestaoPagamentosServiceTest.java, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-RESEARCH.md</read_first>
  <action>Add focused unit tests with in-memory fixtures for the comparator. Cover at least: equal snapshots produce no divergences; text mismatch is reported; monetary mismatch is reported; date/status mismatch is reported; missing product/parcel/rateio rows are reported; ordering mismatch is reported; pending origin is reported when a field cannot be compared because the origin is absent. Keep these tests independent of the real legacy database and independent of Vaadin.</action>
  <verify>mvn test -Dtest=*Validation*Test,*GestaoPagamentos*Test</verify>
  <acceptance_criteria>
    - Tests prove exact parity succeeds.
    - Tests prove representative divergences are not swallowed.
    - Tests include products, parcels and rateios, not only the title grid.
    - Tests run without SALOME_LEGACY_DB_* variables.
  </acceptance_criteria>
  <done>Comparator behavior is guarded by database-free fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Implement expected legacy reads and actual service collection</name>
  <files>src/main/java/br/com/salome/core/application/contaspagar/validation/**, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/**, src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java</files>
  <read_first>.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md, .planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java, src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java</read_first>
  <action>Create the live validation collection path. Build expected snapshots from read-only legacy SQL grounded in the mapped origins for `NotaCompra`, `NotaCompraDuplicatas`, `NotaCompraProdutos`, `NotaCompraRateio` and `Extrato`. Build actual snapshots by calling `GestaoPagamentosService`/`GestaoPagamentosRepository` for the same filters and selected keys. Run all required scenarios: `Hoje`, `Vencidas`, `Proximos dias`, `Todas` and a manual period. Ensure the validation path processes every row returned by each scenario rather than the UI's display limit. If the current adapter has query blockers, such as `SQL_TITULOS` selecting `valorTotal` while the mapper reads `valorNota`, fix only the narrow read bug needed for truthful validation and record it in the summary/report.</action>
  <verify>rg -n "HOJE|VENCIDAS|PROXIMOS_DIAS|TODAS|PERIODO_MANUAL|notacompraduplicatas|notacompraprodutos|notacomprarateio|extrato" src/main/java/br/com/salome/core</verify>
  <acceptance_criteria>
    - Expected live reads cite legacy origins for title, detail, products, parcels and rateio.
    - Actual reads use the same service/repository path as the Vaadin screen.
    - All required filters are collected.
    - Live validation does not use in-memory fallback data.
    - Any narrow read fix is scoped, documented and covered by tests or validation output.
  </acceptance_criteria>
  <done>Live validation can compare legacy expected data with salome-core actual data.</done>
</task>

<task type="auto">
  <name>Task 4: Add local validation runner and Markdown report writer</name>
  <files>src/main/java/br/com/salome/core/application/contaspagar/validation/**, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/**, docs/setup/salome-core-local.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md</files>
  <read_first>docs/setup/salome-core-local.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-CONTEXT.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-RESEARCH.md</read_first>
  <action>Add a local validation runner outside the Vaadin UI, such as a Spring profile/property-driven command or application runner. The runner must check the legacy datasource configuration before doing any comparison. If `SALOME_LEGACY_DB_ENABLED`, URL, username or password are missing, fail with a clear message listing the missing variables and do not pretend validation passed. When configured, run every scenario, compare all returned records and write `08-VALIDATION.md` with status, environment, totals, divergence counts, representative evidence rows, full divergence list or summarized examples, pending-origin questions and follow-up recommendations. Update local setup docs with the exact command and env vars.</action>
  <verify>rg -n "08-VALIDATION|SALOME_LEGACY_DB_ENABLED|SALOME_LEGACY_DB_URL|validation|VALIDATION" src/main/java docs/setup/salome-core-local.md .planning/phases/08-validar-dados-da-tela-web-contra-o-legado</verify>
  <acceptance_criteria>
    - Validation is runnable locally without adding a Vaadin button.
    - Missing DB configuration fails loudly and clearly.
    - Successful live run writes `08-VALIDATION.md`.
    - Report includes totals by filter and divergence examples with expected/actual/path/origin.
    - Report does not dump the entire database when volumes are high.
  </acceptance_criteria>
  <done>A local validation command can produce the versioned Phase 8 report.</done>
</task>

<task type="auto">
  <name>Task 5: Run final checks and document execution</name>
  <files>.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-01-SUMMARY.md</files>
  <read_first>AGENTS.md, .planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-CONTEXT.md</read_first>
  <action>Run the automated tests and safety checks. Run the live validation command if legacy DB credentials are configured in the local environment. If credentials or Java/Maven tooling are unavailable, record the exact blocker in `08-VALIDATION.md` and `08-01-SUMMARY.md` instead of marking parity as passed. Confirm UI still has no SQL/JDBC, legacy adapter code still has no mutating operations, Flyway remains disabled for legacy DB safety, and tracked files under `salome-legacy` are unchanged.</action>
  <verify>mvn test; if (rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/ui) { exit 1 }; if (rg -n "INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar) { exit 1 }; rg -n "enabled: false|flyway" src/main/resources/application.yml docs/setup/salome-core-local.md; git status --short --untracked-files=no salome-legacy</verify>
  <acceptance_criteria>
    - `mvn test` passes with JDK 25/Maven or the exact tooling blocker is documented.
    - Live validation produces a real report when legacy DB credentials are configured.
    - Missing legacy DB config produces an explicit blocked validation state, not success.
    - UI layer has no SQL/JdbcTemplate usage.
    - Legacy Contas a Pagar adapter has no mutating operations.
    - No tracked file under `salome-legacy` is modified.
    - `08-01-SUMMARY.md` documents files changed, origins preserved, tests run and remaining divergences/blockers.
  </acceptance_criteria>
  <done>Phase 8 validation is tested, reported and safe for review before write phases.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test` passes with JDK 25 and without legacy DB credentials, or the exact tooling blocker is documented.
- [ ] Comparator fixture tests cover equal snapshots, text/date/money/status mismatch, missing rows, ordering mismatch and pending-origin fields.
- [ ] Live validation command fails clearly when `SALOME_LEGACY_DB_ENABLED`, `SALOME_LEGACY_DB_URL`, `SALOME_LEGACY_DB_USERNAME` or `SALOME_LEGACY_DB_PASSWORD` are missing.
- [ ] Live validation command processes `Hoje`, `Vencidas`, `Proximos dias`, `Todas` and manual period when the legacy DB is configured.
- [ ] `.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-VALIDATION.md` exists and records totals, divergences, pending origins and evidence status.
- [ ] `rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/ui` returns no matches.
- [ ] `rg -n "INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar` returns no mutating operations.
- [ ] `rg -n "enabled: false|flyway" src/main/resources/application.yml docs/setup/salome-core-local.md` confirms Flyway safety remains visible.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked modifications.
</verification>

<success_criteria>
- READ-03 has a reusable validation path for comparing the web read model against the legacy MySQL/mapped behavior.
- Every required filter is validated: Hoje, Vencidas, Proximos dias, Todas and manual period.
- Main grid and full details are compared: titles, selected detail, products, parcels and rateio.
- Divergences are strict and traceable, with expected value, actual value, path and origin.
- `08-VALIDATION.md` is versioned and reviewable.
- The comparator is tested with fixtures without live MySQL.
- Missing live DB configuration is reported honestly as blocked.
- No writes, baixa, edit, delete, mutable rateio or payment operations are introduced.
- No `salome-legacy` code or production schema is changed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-validar-dados-da-tela-web-contra-o-legado/08-01-SUMMARY.md`.
</output>
