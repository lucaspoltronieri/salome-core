---
phase: 13-criar-fluxo-de-caixa-previsto-operacional
plan: 01
type: execute
wave: 1
depends_on:
  - phase-12
  - phase-11
  - phase-10
files_modified:
  - src/main/java/br/com/salome/core/domain/financeiro/**
  - src/main/java/br/com/salome/core/application/financeiro/**
  - src/main/java/br/com/salome/core/infrastructure/legacy/financeiro/**
  - src/test/java/br/com/salome/core/application/financeiro/**
autonomous: true
requirements:
  - CASH-01
user_setup:
  - "Use only safe development or homologation data sources."
  - "Do not modify salome-legacy."
must_haves:
  truths:
    - "D-01 is covered: the forecast is payables-driven and uses NotaCompraDuplicatas as the main projection source."
    - "D-02 is covered: opening balance is treated as operational context using v_saldobancariotalao when needed."
    - "D-03 is covered: the main result is a cumulative operational timeline with projected balances and realized items."
    - "D-07 is covered: the default window is the current month and the forecast horizon is user-configurable."
    - "D-08 is covered: filters reuse the existing financial semantics for period, filial, fornecedor, banco and plano de contas with manual refresh only."
    - "D-09 is covered: the current-user context can seed the initial recorte, while the user can still switch to Todos."
    - "No SQL or business rule is added to a Vaadin View."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "src/main/java/br/com/salome/core/domain/financeiro/FluxoCaixaPrevisto*.java"
      provides: "Forecast read model contracts"
      contains: "periodo"
    - path: "src/main/java/br/com/salome/core/application/financeiro/FluxoCaixaPrevistoService.java"
      provides: "Forecast service boundary"
      contains: "consultar"
    - path: "src/main/java/br/com/salome/core/application/financeiro/FluxoCaixaPrevistoRepository.java"
      provides: "Forecast repository port"
      contains: "listar"
    - path: "src/main/java/br/com/salome/core/infrastructure/legacy/financeiro/LegacyFluxoCaixaPrevistoRepository.java"
      provides: "Legacy projection adapter"
      contains: "notacompraduplicatas"
    - path: "src/test/java/br/com/salome/core/application/financeiro/FluxoCaixaPrevistoServiceTest.java"
      provides: "Forecast service semantics"
      contains: "current month"
---

<objective>
Build the forecast domain contracts and service boundary for phase 13 so the core can compute an operational cash-flow projection from payables, realized payments and opening bank context without putting SQL in the view layer.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-CONTEXT.md
@.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-RESEARCH.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
@.planning/codebase/USUARIO-ACESSO-MAPA.md
@docs/architecture/salome-core-architecture.md
@docs/baixa-contas-pagar-regras-migradas.md
@src/main/java/br/com/salome/core/domain/notacompra/LegacyOrigin.java
@src/main/java/br/com/salome/core/security/CurrentUserContext.java
@src/main/java/br/com/salome/core/domain/financeiro/DashboardFinanceiroFiltro.java
@src/main/java/br/com/salome/core/application/financeiro/DashboardFinanceiroService.java
@src/main/java/br/com/salome/core/infrastructure/legacy/financeiro/LegacyDashboardFinanceiroRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create forecast domain contracts</name>
  <files>src/main/java/br/com/salome/core/domain/financeiro/**</files>
  <action>Create immutable forecast read-model types for the cash-flow phase, including a filter with period, forecast horizon, filial, fornecedor, banco, plano de contas and status, plus snapshot/read-model records for the operational timeline, summary totals and drill-down rows. Keep the contracts independent from Vaadin and JDBC, and attach legacy origin metadata to every traceable row.</action>
  <verify>rg -n "FluxoCaixaPrevisto|FluxoCaixa|horizonte|periodo|LegacyOrigin" src/main/java/br/com/salome/core/domain/financeiro</verify>
  <acceptance_criteria>
    - The filter supports default current-month behavior and user-configurable horizon.
    - The read models can represent projected balances, realized outflows and per-day operational rows.
    - No UI or SQL dependency leaks into the domain package.
  </acceptance_criteria>
  <done>Forecast domain contracts exist and can drive a read-only projection.</done>
</task>

<task type="auto">
  <name>Task 2: Add forecast service and legacy projection adapter</name>
  <files>src/main/java/br/com/salome/core/application/financeiro/** src/main/java/br/com/salome/core/infrastructure/legacy/financeiro/**</files>
  <action>Create `FluxoCaixaPrevistoService` and a `FluxoCaixaPrevistoRepository` port with a `LegacyFluxoCaixaPrevistoRepository` adapter. The adapter should read the legacy source of truth from `notacompraduplicatas`, `notacompra`, `extrato`, `banco`, `filial`, `fornecedor`, `planocontascentrocusto`, `planocontas` and `v_saldobancariotalao`, then the service should compute the opening balance, projected daily movements and summary totals. Keep all SQL in the adapter and all projection logic in the service boundary.</action>
  <verify>rg -n "FluxoCaixaPrevistoService|FluxoCaixaPrevistoRepository|LegacyFluxoCaixaPrevistoRepository|notacompraduplicatas|notacompra|extrato|v_saldobancariotalao|planocontascentrocusto|planocontas" src/main/java/br/com/salome/core/application/financeiro src/main/java/br/com/salome/core/infrastructure/legacy/financeiro</verify>
  <acceptance_criteria>
    - SQL lives only in `infrastructure.legacy`.
    - Projection semantics derive from the legacy payables flow and not from `contaspagar` dead code.
    - The service composes the read model without any Vaadin imports.
    - The forecast keeps operational meaning by exposing projected balance evolution and realized-versus-expected items.
  </acceptance_criteria>
  <done>Forecast data can be loaded as a single service call.</done>
</task>

<task type="auto">
  <name>Task 3: Add service-level tests for forecast semantics</name>
  <files>src/test/java/br/com/salome/core/application/financeiro/**</files>
  <action>Add database-free tests with fake repository data proving the forecast semantics: current-month default, configurable horizon, ordered operational timeline, opening balance application, projected-vs-realized separation and manual refresh through repeated service calls. Keep the tests independent from MySQL and focus on projection correctness rather than browser automation.</action>
  <verify>mvn test -Dtest=*FluxoCaixaPrevisto*Test,*Financeiro*Test -DskipFrontendBuild</verify>
  <acceptance_criteria>
    - Tests run without MySQL.
    - The forecast timeline is deterministic and derived from the supplied fake repository rows.
    - The service keeps the current-month default and horizon semantics stable.
  </acceptance_criteria>
  <done>Forecast read-model semantics are protected by tests.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test -Dtest=*FluxoCaixaPrevisto*Test,*Financeiro*Test -DskipFrontendBuild` passes or the exact blocker is documented.
- [ ] `rg -n "SELECT|INSERT|UPDATE|DELETE|JdbcTemplate" src/main/java/br/com/salome/core/domain` returns no matches.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked legacy modifications.
</verification>

<success_criteria>
- Forecast domain/application/infrastructure boundaries exist.
- A single service snapshot can describe the projected cash-flow timeline and summary totals.
- Phase 13 requirements are traceable to legacy origin and not to `contaspagar` dead code.
- The forecast remains read-only and routes drill-down only to operational screens.
</success_criteria>

<output>
After completion, create `.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-01-SUMMARY.md`.
</output>
