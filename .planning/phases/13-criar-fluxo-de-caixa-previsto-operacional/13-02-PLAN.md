---
phase: 13-criar-fluxo-de-caixa-previsto-operacional
plan: 02
type: execute
wave: 2
depends_on:
  - phase-12
  - phase-11
  - phase-10
files_modified:
  - src/main/java/br/com/salome/core/ui/**
  - src/test/java/br/com/salome/core/ui/**
autonomous: true
requirements:
  - CASH-01
user_setup:
  - "Use only safe development or homologation data sources."
  - "Do not modify salome-legacy."
must_haves:
  truths:
    - "D-04 is covered: the placeholder route is replaced by an operational forecast view and the UI stays dense, not decorative."
    - "D-05 is covered: the forecast view does not execute baixa, reversao, inclusao or pagamento."
    - "D-06 is covered: drill-down from forecast items reaches GestaoPagamentosView, DocumentoEntradaView or DocumentoEntradaDetalhesView as appropriate."
    - "D-10 is covered: the primary presentation favors the chronological operational timeline with near-term emphasis."
    - "D-11 is covered: the UI differentiates realized items from still-forecast items without duplicating baixa logic."
    - "D-12 is covered: refresh remains manual only to avoid operational noise."
    - "No SQL or business rule is added to a Vaadin View."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "src/main/java/br/com/salome/core/ui/placeholder/FluxoCaixaView.java"
      provides: "Route replacement target"
      contains: "Fluxo de Caixa"
    - path: "src/main/java/br/com/salome/core/ui/financeiro/FluxoCaixaPrevistoView.java"
      provides: "Operational forecast screen"
      contains: "Atualizar"
    - path: "src/main/java/br/com/salome/core/ui/MainLayout.java"
      provides: "Drawer navigation entry"
      contains: "Fluxo de Caixa"
    - path: "src/test/java/br/com/salome/core/ui/**"
      provides: "UI verification"
      contains: "forecast"
---

<objective>
Build the Vaadin forecast screen and navigation integration for phase 13, using the forecast read model from plan 01 and keeping all drill-down actions read-only.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-CONTEXT.md
@.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-RESEARCH.md
@docs/architecture/salome-core-architecture.md
@src/main/java/br/com/salome/core/ui/MainLayout.java
@src/main/java/br/com/salome/core/ui/placeholder/FluxoCaixaView.java
@src/main/java/br/com/salome/core/ui/financeiro/DashboardFinanceiroView.java
@src/main/java/br/com/salome/core/application/financeiro/FluxoCaixaPrevistoService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the forecast Vaadin view</name>
  <files>src/main/java/br/com/salome/core/ui/**</files>
  <action>Replace the placeholder flux-caixa route with a real operational forecast view that renders a dense timeline, summary totals, manual refresh controls and a short list of forecast rows. Keep the layout practical and read-only, and consume the forecast service read model without SQL or business rules in the view layer.</action>
  <verify>rg -n "FluxoCaixaPrevistoView|FluxoCaixaView|Atualizar|Filtrar|navigate|timeline|previsto" src/main/java/br/com/salome/core/ui</verify>
  <acceptance_criteria>
    - The first fold shows operational information, not decorative cards.
    - The view can render the full forecast snapshot without mutating state.
    - Manual refresh is the only refresh behavior exposed in the UI.
  </acceptance_criteria>
  <done>Forecast screen exists and renders the operational projection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation and drill-down</name>
  <files>src/main/java/br/com/salome/core/ui/MainLayout.java src/main/java/br/com/salome/core/ui/**</files>
  <action>Update the drawer entry and view wiring so the forecast screen opens from the main navigation. Wire click actions from forecast items to the existing operational screens only, preserving the read-only boundary and keeping correction flows inside the owning views.</action>
  <verify>rg -n "Fluxo de Caixa|GestaoPagamentosView|DocumentoEntradaView|DocumentoEntradaDetalhesView|navigate" src/main/java/br/com/salome/core/ui/MainLayout.java src/main/java/br/com/salome/core/ui</verify>
  <acceptance_criteria>
    - The forecast opens from the app navigation.
    - Clicks route to existing operational screens with the right context.
    - No mutation action is exposed in the forecast navigation.
  </acceptance_criteria>
  <done>Forecast navigation and drill-down paths are wired.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI verification and compile checks</name>
  <files>src/test/java/br/com/salome/core/ui/**</files>
  <action>Add lightweight UI-facing verification where practical and document the expected manual checks for the forecast first fold, filters, drill-down and no-write behavior. Focus the automated coverage on compile-level safety and service wiring rather than browser-only logic. Keep the tests independent from production data.</action>
  <verify>mvn test -Dtest=*FluxoCaixaPrevisto*Test,*UI*Test -DskipFrontendBuild</verify>
  <acceptance_criteria>
    - The forecast view compiles against the service read model.
    - Navigation changes do not break the existing main layout.
    - Manual behavior checks are documented and repeatable.
  </acceptance_criteria>
  <done>Forecast UI is verified enough for phase handoff.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test -Dtest=*FluxoCaixaPrevisto*Test,*UI*Test -DskipFrontendBuild` passes or the exact blocker is documented.
- [ ] `rg -n "SELECT|INSERT|UPDATE|DELETE|JdbcTemplate" src/main/java/br/com/salome/core/ui` returns no matches.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked legacy modifications.
</verification>

<success_criteria>
- The forecast is visible from the app navigation.
- The forecast shows a dense operational timeline with summary and drill-down information.
- Drill-down routes go only to operational screens.
- Phase 13 remains read-only and does not create new financial mutation paths.
</success_criteria>

<output>
After completion, create `.planning/phases/13-criar-fluxo-de-caixa-previsto-operacional/13-02-SUMMARY.md`.
</output>
