---
phase: 06-criar-projeto-java-25-spring-boot-4-vaadin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/br/com/salome/core/**
  - src/main/resources/application.yml
  - src/main/resources/application-local.yml
  - src/main/resources/db/migration/.gitkeep
  - src/test/java/br/com/salome/core/**
  - docs/setup/salome-core-local.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
user_setup:
  - "Install/use JDK 25 for local compilation."
  - "To run against the legacy MySQL database, provide SALOME_LEGACY_DB_URL, SALOME_LEGACY_DB_USERNAME and SALOME_LEGACY_DB_PASSWORD in the active environment."
must_haves:
  truths:
    - "Decision D-01 is covered: salome-core starts as a single Maven module."
    - "Decision D-02 is covered: top-level package roots are ui, application, domain, infrastructure and security."
    - "Decision D-03 is covered: contaspagar subpackages are prepared inside the relevant layers."
    - "Decision D-04 is covered: shared exists only for disciplined cross-cutting code."
    - "Decision D-05 is covered: the app opens directly on Gestao de Pagamentos."
    - "Decision D-06 is covered: the Vaadin shell is a complete, interactive, close-but-not-copy visual model based on references/ux-frontend."
    - "Decision D-07 is covered: side menu includes clickable placeholders for future modules."
    - "Decision D-08 is covered: Gestao de Pagamentos has top filters/actions, left list, right detail/product area and bottom Central de Pagamentos shell."
    - "Decision D-09 is covered: product labels include Gestao de Pagamentos, Central de Pagamentos, Documento de Entrada, Painel Financeiro, Fluxo de Caixa and Movimento Financeiro."
    - "Decision D-10 is covered: the project supports real read access to the legacy MySQL database."
    - "Decision D-11 is covered: functional writes to the legacy database are blocked in this phase."
    - "Decision D-12 is covered: datasource configuration uses profiles and external environment variables."
    - "Decision D-13 is covered: Flyway is installed/configured but does not manage or alter the legacy MySQL database."
    - "Decision D-14 is covered: future legacy schema changes require reviewed versioned SQL executed outside the app by the responsible team."
    - "Decision D-15 is covered: legacy access is implemented through domain-oriented adapters/repositories."
    - "Decision D-16 is covered: the first read slice is limited to Gestao de Pagamentos."
    - "Decision D-17 is covered: Spring Security foundation exists without real login requirement."
    - "Decision D-18 is covered: current user is represented by a fixed development user."
    - "Decision D-19 is covered: basic roles/profiles are prepared without real permission blocking."
    - "Decision D-20 is covered: a contract/interface exists for future legacy user adapter integration."
    - "Vaadin Views call Services; Views do not contain SQL or JdbcTemplate usage."
    - "Services call Repositories/Adapters for legacy data access."
    - "Repositories/Adapters access the legacy MySQL database through Spring JDBC/JdbcTemplate."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "pom.xml"
      provides: "Java 25 + Spring Boot 4 + Vaadin Maven project"
      contains: "maven.compiler.release"
    - path: "src/main/java/br/com/salome/core/SalomeCoreApplication.java"
      provides: "Spring Boot application entry point"
      contains: "@SpringBootApplication"
    - path: "src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java"
      provides: "Initial Vaadin screen for Gestao de Pagamentos"
      contains: "@Route"
    - path: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      provides: "Application service boundary for the first read slice"
      contains: "GestaoPagamentosRepository"
    - path: "src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java"
      provides: "Read-only legacy MySQL adapter for Gestao de Pagamentos"
      contains: "JdbcTemplate"
    - path: "src/main/java/br/com/salome/core/security/CurrentUserContext.java"
      provides: "Current-user contract for security foundation"
      contains: "currentUser"
    - path: "docs/setup/salome-core-local.md"
      provides: "Local run and legacy DB safety documentation"
      contains: "SALOME_LEGACY_DB_URL"
  key_links:
    - from: "src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java"
      to: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      via: "constructor injection"
      pattern: "GestaoPagamentosService"
    - from: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      to: "src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java"
      via: "GestaoPagamentosRepository port"
      pattern: "GestaoPagamentosRepository"
---

<objective>
Create the Phase 6 technical foundation for `salome-core`.

Purpose: bootstrap a Java 25 + Spring Boot 4 + Vaadin Maven project that respects the Phase 5 architecture contract, opens directly on the new **Gestao de Pagamentos** experience, reads the first minimal slice from the legacy MySQL database through Services and Repositories/Adapters, and prepares Spring Security/Flyway without enabling risky writes or migrations against the legacy database.

Decision coverage: this plan translates D-01 through D-20 from `06-CONTEXT.md` into executable scaffolding, configuration, UI shell, read-only legacy integration and verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md
@.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-RESEARCH.md
@docs/architecture/salome-core-architecture.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/USUARIO-ACESSO-MAPA.md
@references/ux-frontend/screens/contas-pagar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Maven Spring Boot Vaadin project</name>
  <files>pom.xml, src/main/java/br/com/salome/core/SalomeCoreApplication.java, src/test/java/br/com/salome/core/SalomeCoreApplicationTests.java</files>
  <read_first>.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-RESEARCH.md, docs/architecture/salome-core-architecture.md</read_first>
  <action>Create the root Maven project. Use a single module with group/artifact coordinates appropriate for `br.com.salome:salome-core`. Configure Java 25 explicitly with `maven.compiler.release` set to `25`. Use Spring Boot 4, Vaadin 25 compatible with Spring Boot 4, and Maven packaging `jar`. Add dependencies for Vaadin Spring Boot starter, Spring Boot JDBC, Spring Security, validation, MySQL Connector/J, Flyway and tests. Add the Spring Boot Maven plugin and Vaadin Maven plugin. Create `SalomeCoreApplication` under `br.com.salome.core` with `@SpringBootApplication`. Add a basic context-load test. Do not create or modify files under `salome-legacy`.</action>
  <verify>Test-Path pom.xml; Test-Path src/main/java/br/com/salome/core/SalomeCoreApplication.java; rg -n "maven.compiler.release|spring-boot|vaadin|mysql|flyway" pom.xml</verify>
  <acceptance_criteria>
    - `pom.xml` exists at repository root.
    - `pom.xml` configures Java release 25.
    - `pom.xml` includes Spring Boot, Vaadin, JDBC, Security, MySQL and Flyway dependencies/plugins.
    - `SalomeCoreApplication.java` exists and contains `@SpringBootApplication`.
    - `salome-legacy/` is not modified.
  </acceptance_criteria>
  <done>The project compiles as a single Maven Spring Boot/Vaadin module skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Add layered package structure and configuration profiles</name>
  <files>src/main/java/br/com/salome/core/**, src/main/resources/application.yml, src/main/resources/application-local.yml, src/main/resources/db/migration/.gitkeep, docs/setup/salome-core-local.md</files>
  <read_first>.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md, docs/architecture/salome-core-architecture.md</read_first>
  <action>Create package roots for `ui`, `application`, `domain`, `infrastructure`, `security` and `shared`. Prepare `contaspagar` subpackages under `ui`, `application`, `domain` and `infrastructure.legacy`. Add application configuration using Spring profiles and external environment variables for the legacy datasource: `SALOME_LEGACY_DB_URL`, `SALOME_LEGACY_DB_USERNAME`, `SALOME_LEGACY_DB_PASSWORD`. Add `application.yml` and `application-local.yml` so the project can start with profile-based configuration. Include a `db/migration/.gitkeep` folder for future governance, but configure Flyway so it is not applied to the legacy MySQL datasource in this phase. Document local startup, env vars, and the rule that Flyway must not manage or alter the legacy MySQL database in `docs/setup/salome-core-local.md`.</action>
  <verify>rg -n "SALOME_LEGACY_DB_URL|spring.flyway.enabled|Gestao de Pagamentos" src/main/resources docs/setup/salome-core-local.md; Test-Path src/main/resources/db/migration/.gitkeep</verify>
  <acceptance_criteria>
    - Package roots for all required layers exist.
    - `contaspagar` subpackages exist under the relevant layers.
    - Configuration reads legacy DB credentials from environment variables.
    - Flyway is present but disabled/safe for the legacy datasource in this phase.
    - `docs/setup/salome-core-local.md` states that no Flyway migration may run against the legacy MySQL database.
  </acceptance_criteria>
  <done>Layered structure and safe configuration profiles are ready.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the read-only Gestao de Pagamentos application boundary</name>
  <files>src/main/java/br/com/salome/core/application/contaspagar/**, src/main/java/br/com/salome/core/domain/contaspagar/**, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/**</files>
  <read_first>.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md, .planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md</read_first>
  <action>Create read models for the first `Gestao de Pagamentos` slice, such as title/list row, selected-title detail, product/detail row and payment summary. Create a `GestaoPagamentosRepository` port in the application layer and implement it in `LegacyGestaoPagamentosRepository` using Spring JDBC/JdbcTemplate. Keep repository methods read-only only: list/filter titles, load selected title detail and load supporting rows needed by the initial screen. Use SQL based on the mapped legacy tables `notacompra`, `notacompraduplicatas`, `fornecedor`, `filial`, `extrato`, and related lookup tables only as needed for the screen. Create `GestaoPagamentosService` that coordinates the read model and marks read operations with read-only transaction semantics when applicable. Do not expose write, baixa, delete, save, rateio mutation or payment commands in this phase.</action>
  <verify>rg -n "interface GestaoPagamentosRepository|class LegacyGestaoPagamentosRepository|JdbcTemplate|readOnly|notacompraduplicatas|notacompra|fornecedor" src/main/java/br/com/salome/core</verify>
  <acceptance_criteria>
    - `GestaoPagamentosService` exists and depends on a repository/adapter port.
    - `LegacyGestaoPagamentosRepository` exists and uses `JdbcTemplate`.
    - Vaadin UI classes do not contain SQL strings or `JdbcTemplate`.
    - Repository methods are read-only and do not include insert/update/delete operations.
    - The first read slice is limited to `Gestao de Pagamentos`.
  </acceptance_criteria>
  <done>Gestao de Pagamentos has a read-only service/repository boundary over the legacy schema.</done>
</task>

<task type="auto">
  <name>Task 4: Add security foundation and current-user contracts</name>
  <files>src/main/java/br/com/salome/core/security/**, src/main/java/br/com/salome/core/infrastructure/legacy/security/**</files>
  <read_first>.planning/codebase/USUARIO-ACESSO-MAPA.md, docs/architecture/salome-core-architecture.md</read_first>
  <action>Create a security foundation without requiring real legacy login. Add `CurrentUserContext`, a current user read model, role/authority constants for basic future permissions and a development implementation that returns a fixed technical user. Add a Spring Security configuration that permits local phase-6 access while keeping Spring Security active in the project. Create an interface/contract for the future legacy user adapter, such as `LegacyUserAdapter` or `LegacyUserRepository`, but do not implement real password validation or login against the legacy database. Ensure user-facing permissions are not used yet to block the shell routes.</action>
  <verify>rg -n "CurrentUserContext|Development|ROLE_|SecurityFilterChain|LegacyUser" src/main/java/br/com/salome/core/security src/main/java/br/com/salome/core/infrastructure/legacy/security</verify>
  <acceptance_criteria>
    - Spring Security is configured and active.
    - App access does not require real login in Phase 6.
    - A fixed development user exists through `CurrentUserContext`.
    - Basic roles/authorities are represented for future mapping.
    - A future legacy user adapter contract exists without real authentication integration.
  </acceptance_criteria>
  <done>Security is prepared without blocking Phase 6 on unknown legacy authentication details.</done>
</task>

<task type="auto">
  <name>Task 5: Build the Vaadin shell and Gestao de Pagamentos view</name>
  <files>src/main/java/br/com/salome/core/ui/**</files>
  <read_first>references/ux-frontend/screens/contas-pagar.jsx, .planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md</read_first>
  <action>Create a Vaadin `AppLayout` or equivalent main layout with a side menu. The root route must open `Gestao de Pagamentos`. Add clickable placeholder routes/pages for `Documento de Entrada`, `Painel Financeiro`, `Fluxo de Caixa` and `Movimento Financeiro`. Implement `GestaoPagamentosView` as a dense operational screen inspired by the reference, but implemented natively in Vaadin and not copied from React. Include top filters/actions, a left list/grid of titles/duplicatas, a right detail/product area, and a bottom `Central de Pagamentos` shell. Wire the view to `GestaoPagamentosService`. Mutating actions such as save, baixa, excluir, confirmar, rateio or payment execution must be disabled or placeholder-only in this phase. Keep SQL and financial rules out of the View.</action>
  <verify>rg -n "@Route|Gestao de Pagamentos|Documento de Entrada|Painel Financeiro|Fluxo de Caixa|Movimento Financeiro|Central de Pagamentos" src/main/java/br/com/salome/core/ui; if (rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE" src/main/java/br/com/salome/core/ui) { exit 1 }</verify>
  <acceptance_criteria>
    - Root route opens `Gestao de Pagamentos`.
    - Side menu contains clickable future module placeholders.
    - `GestaoPagamentosView` calls `GestaoPagamentosService`.
    - The UI contains no SQL and no `JdbcTemplate` usage.
    - Mutating operations are not functional in this phase.
  </acceptance_criteria>
  <done>The initial Vaadin application opens on a real, service-backed Gestao de Pagamentos shell.</done>
</task>

<task type="auto">
  <name>Task 6: Add tests and guardrail checks</name>
  <files>src/test/java/br/com/salome/core/**</files>
  <read_first>.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md, .planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-RESEARCH.md</read_first>
  <action>Add focused tests that verify the application context, the development current user, the read-only service boundary with a fake repository, and the absence of write command exposure for Phase 6 where practical. Keep tests independent of real MySQL credentials. If a live DB smoke check is added, gate it behind an explicit profile/env variable so normal `mvn test` does not require access to the VPS or production-like database.</action>
  <verify>mvn test; rg -n "Development|GestaoPagamentosService|CurrentUserContext" src/test/java/br/com/salome/core</verify>
  <acceptance_criteria>
    - `mvn test` passes without legacy DB credentials.
    - Tests cover `CurrentUserContext` development user behavior.
    - Tests cover `GestaoPagamentosService` read orchestration using a fake repository.
    - No test mutates the legacy database.
  </acceptance_criteria>
  <done>Phase 6 foundation has fast tests and safety guardrails.</done>
</task>

<task type="auto">
  <name>Task 7: Verify build, legacy safety and documentation</name>
  <files>docs/setup/salome-core-local.md, .planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-01-SUMMARY.md</files>
  <read_first>AGENTS.md, .planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md</read_first>
  <action>Run the final verification commands. Confirm Maven tests pass, Vaadin/UI classes contain no SQL/JdbcTemplate, repository classes contain no insert/update/delete methods for this phase, Flyway is not configured to target the legacy datasource, and `salome-legacy` has no modifications. Update `docs/setup/salome-core-local.md` with the run command, required JDK, env vars, and explicit statement that any future legacy schema change must be versioned SQL reviewed and executed outside the app by the responsible team. Create `06-01-SUMMARY.md` after successful execution.</action>
  <verify>mvn test; git status --short --untracked-files=no salome-legacy; rg -n "Flyway|SALOME_LEGACY_DB_URL|nao.*legado|versionado" docs/setup/salome-core-local.md</verify>
  <acceptance_criteria>
    - `mvn test` passes.
    - `git status --short --untracked-files=no salome-legacy` shows no tracked modifications.
    - UI layer has no SQL or JDBC access.
    - Documentation explains local run and database safety.
    - `06-01-SUMMARY.md` exists after execution.
  </acceptance_criteria>
  <done>Phase 6 is verifiable, documented and ready for execution review.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test` passes with JDK 25 and without legacy DB credentials.
- [ ] `rg -n "maven.compiler.release|spring-boot|vaadin" pom.xml` confirms the Java/Spring/Vaadin foundation.
- [ ] `rg -n "@Route|Gestao de Pagamentos|Central de Pagamentos" src/main/java/br/com/salome/core/ui` confirms the Vaadin shell.
- [ ] `rg -n "GestaoPagamentosService|GestaoPagamentosRepository|LegacyGestaoPagamentosRepository" src/main/java/br/com/salome/core` confirms View -> Service -> Repository/Adapter boundaries.
- [ ] `rg -n "SELECT|JdbcTemplate" src/main/java/br/com/salome/core/ui` returns no matches.
- [ ] `rg -n "INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar` returns no mutating repository operations for Phase 6.
- [ ] `rg -n "spring.flyway.enabled: false|spring.flyway.enabled=false" src/main/resources docs/setup/salome-core-local.md` confirms Flyway is not running against legacy MySQL in this phase.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked modifications.
</verification>

<success_criteria>
- The repository contains a single-module Maven project for Java 25, Spring Boot 4 and Vaadin.
- The code structure follows the approved layers: `ui`, `application`, `domain`, `infrastructure`, `security`, plus disciplined `shared`.
- The app opens directly on `Gestao de Pagamentos`.
- The Vaadin shell includes side-menu placeholders for future modules.
- The first read slice uses a Service and a domain-oriented legacy Repository/Adapter.
- The legacy MySQL connection is read-only from application behavior in this phase.
- Flyway is present for governance but does not alter or manage the legacy MySQL database.
- Spring Security is present with a fixed development current user and future legacy adapter contract.
- No code under `salome-legacy` is changed.
- Phase requirements ARCH-01, ARCH-02, ARCH-03 and ARCH-04 are covered by executable scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-01-SUMMARY.md`.
</output>
