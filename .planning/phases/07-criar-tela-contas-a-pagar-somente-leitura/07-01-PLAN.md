---
phase: 07-criar-tela-contas-a-pagar-somente-leitura
plan: 01
type: execute
wave: 1
depends_on:
  - phase-06
files_modified:
  - src/main/java/br/com/salome/core/domain/contaspagar/**
  - src/main/java/br/com/salome/core/application/contaspagar/**
  - src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/**
  - src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java
  - src/test/java/br/com/salome/core/application/contaspagar/**
  - docs/setup/salome-core-local.md
autonomous: true
requirements:
  - READ-01
  - READ-02
user_setup:
  - "Use JDK 25 when running Maven locally."
  - "Legacy DB validation remains optional and requires SALOME_LEGACY_DB_ENABLED=true plus SALOME_LEGACY_DB_URL, SALOME_LEGACY_DB_USERNAME and SALOME_LEGACY_DB_PASSWORD."
must_haves:
  truths:
    - "Decision D-01 is covered: the central listing is a queue of duplicatas linked to NotaCompra, not a direct Swing clone and not a primary note list."
    - "Decision D-02 is covered: each central row exposes fornecedor, documento/nota, parcela, vencimento, valor, status and filial."
    - "Decision D-03 is covered: the row stays operationally scannable; richer note/product/account information stays in detail areas."
    - "Decision D-04 is covered: initial screen load uses the Hoje filter for duplicatas vencendo hoje."
    - "Decision D-05 is covered: overdue duplicatas are available through Vencidas and are not mixed into the initial Hoje load."
    - "Decision D-06 is covered: quick shortcuts exist for Hoje, Vencidas, Proximos dias and Todas."
    - "Decision D-07 is covered: manual date/period filtering exists in addition to quick shortcuts."
    - "Decision D-08 is covered: the experience has central list, right details and Central de Pagamentos area."
    - "Decision D-09 is covered: the central area concentrates the operational duplicata queue."
    - "Decision D-10 is covered: selecting a duplicata loads note products, values and plano/centro de contas detail."
    - "Decision D-11 is covered: Central de Pagamentos remains prepared but read-only with no baixa enabled."
    - "Vaadin View calls GestaoPagamentosService only; it does not contain SQL or JdbcTemplate."
    - "GestaoPagamentosService coordinates read use cases and calls GestaoPagamentosRepository."
    - "LegacyGestaoPagamentosRepository is the only Phase 7 code that accesses legacy SQL for this screen."
    - "No write, baixa, edit, delete, rateio mutation or payment command is exposed."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "src/main/java/br/com/salome/core/domain/contaspagar/GestaoPagamentosFiltro.java"
      provides: "Filter model for quick shortcuts and manual period"
      contains: "hoje"
    - path: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      provides: "Read-only application service for the screen snapshot"
      contains: "readOnly"
    - path: "src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java"
      provides: "Legacy MySQL read adapter for duplicata queue and details"
      contains: "notacompraduplicatas"
    - path: "src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java"
      provides: "Vaadin read-only Gestao de Pagamentos screen"
      contains: "Central de Pagamentos"
    - path: "src/test/java/br/com/salome/core/application/contaspagar/GestaoPagamentosServiceTest.java"
      provides: "Service/read-only guardrail tests"
      contains: "GestaoPagamentosFiltro"
  key_links:
    - from: "src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java"
      to: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      via: "constructor injection and carregarPainel"
      pattern: "GestaoPagamentosService"
    - from: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java"
      to: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosRepository.java"
      via: "read-only repository port"
      pattern: "listarTitulos"
    - from: "src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosRepository.java"
      to: "src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java"
      via: "Spring repository implementation"
      pattern: "JdbcTemplate"
---

<objective>
Implement the Phase 7 read-only Contas a Pagar screen inside the existing **Gestao de Pagamentos** Vaadin experience.

Purpose: evolve the Phase 6 shell into an operational, service-backed queue of `NotaCompraDuplicatas` linked to `NotaCompra`, with default "Hoje" due-date filtering, quick shortcuts, manual period filtering, selected-title details, products, parcels, rateio/plano de contas information and a blocked Central de Pagamentos area.

Decision coverage: this plan translates D-01 through D-11 from `07-CONTEXT.md` into executable changes while preserving READ-01 and READ-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md
@.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-RESEARCH.md
@.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-CONTEXT.md
@.planning/phases/06-criar-projeto-java-25-spring-boot-4-vaadin/06-01-SUMMARY.md
@docs/architecture/salome-core-architecture.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/USUARIO-ACESSO-MAPA.md
@references/ux-frontend/screens/contas-pagar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model Phase 7 filters and read rows</name>
  <files>src/main/java/br/com/salome/core/domain/contaspagar/**, src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosService.java</files>
  <read_first>.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md, .planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-RESEARCH.md, src/main/java/br/com/salome/core/domain/contaspagar/GestaoPagamentosFiltro.java, src/main/java/br/com/salome/core/domain/contaspagar/TituloResumo.java</read_first>
  <action>Expand `GestaoPagamentosFiltro` so it can represent the quick filters `Hoje`, `Vencidas`, `Proximos dias`, `Todas` and a manual date/period filter. Include an explicit date range or mode field so the repository can apply filters without deriving business meaning inside the Vaadin View. Make `padrao()` return the `Hoje` filter. If needed, expand `TituloResumo` with a parcel label/number field so each grid row clearly represents a duplicata rather than only a note. Update `GestaoPagamentosService` only as needed to accept and propagate the active filter, preserve read-only transaction semantics, and keep selected-detail resolution by nota + duplicata id.</action>
  <verify>rg -n "Hoje|Vencidas|Proximos|Todas|padrao|readOnly" src/main/java/br/com/salome/core/domain/contaspagar src/main/java/br/com/salome/core/application/contaspagar</verify>
  <acceptance_criteria>
    - Default filter maps to "Hoje".
    - Quick filters and manual period can be represented without SQL or business rules in the View.
    - `TituloResumo` still carries nota id and duplicata id.
    - `GestaoPagamentosService` remains read-only and continues to call `GestaoPagamentosRepository`.
  </acceptance_criteria>
  <done>Filter and read-model contracts can express Phase 7's operational queue.</done>
</task>

<task type="auto">
  <name>Task 2: Align the legacy adapter with duplicata-first reading</name>
  <files>src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/InMemoryGestaoPagamentosRepository.java</files>
  <read_first>.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md, .planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md, .planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-RESEARCH.md</read_first>
  <action>Rewrite the title query so `notacompraduplicatas` is the driving table joined to `notacompra`, `fornecedor`, `filial` and optional `extrato`. Apply the active `GestaoPagamentosFiltro`: default "Hoje" should return `ncd.vencimento = CURRENT_DATE`, "Vencidas" should return unpaid items with `vencimento < CURRENT_DATE`, "Proximos dias" should use the configured future range, "Todas" should avoid the date restriction, and manual period should use its start/end. Keep status derived from read columns only: paid when `datapagamento` exists, overdue when unpaid and vencimento is before today, otherwise pending. Avoid row multiplication from `notacomprarateio` by using a correlated subquery, aggregate, or a separate detail query for plano/centro summary. Update the in-memory repository so local fallback data and filtering match the same semantics, including at least one item due on the current date.</action>
  <verify>rg -n "FROM notacompraduplicatas|CURRENT_DATE|datapagamento|vencimento|idNotaCompraDuplicatas|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar</verify>
  <acceptance_criteria>
    - Title query is duplicata-first and returns one row per duplicata.
    - D-02 fields are available to the grid: fornecedor, documento/nota, parcela, vencimento, valor, status and filial.
    - Default `Hoje` and `Vencidas` semantics are implemented in repository/filtering logic, not in the View.
    - Repository remains read-only: no insert/update/delete/executeUpdate.
    - In-memory fallback supports the same quick filters.
  </acceptance_criteria>
  <done>Legacy and local adapters return the Phase 7 operational duplicata queue safely.</done>
</task>

<task type="auto">
  <name>Task 3: Refine the Vaadin Gestao de Pagamentos screen</name>
  <files>src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java</files>
  <read_first>.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md, references/ux-frontend/screens/contas-pagar.jsx, src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java</read_first>
  <action>Update `GestaoPagamentosView` to keep an active `GestaoPagamentosFiltro`, load the screen initially with `GestaoPagamentosFiltro.padrao()`/Hoje, and expose quick shortcuts for `Hoje`, `Vencidas`, `Proximos dias` and `Todas`. Add manual period controls for date range filtering. Make the central grid visibly a duplicata queue, with columns for fornecedor, documento/nota, parcela, vencimento, valor, status and filial. Keep the right panel focused on selected duplicata context: products linked to the note, values, plano/centro de contas and parcel/payment read-only information. Keep Central de Pagamentos present but disabled/read-only with tooltips or copy that make clear baixa, confirmacao, cheque and exclusao are not available in this phase. Do not add SQL, JdbcTemplate or financial mutation logic to the View.</action>
  <verify>rg -n "Hoje|Vencidas|Proximos|Todas|DatePicker|Central de Pagamentos|GestaoPagamentosService" src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java; if (rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/ui/contaspagar/GestaoPagamentosView.java) { exit 1 }</verify>
  <acceptance_criteria>
    - Root Gestao de Pagamentos screen opens in read-only mode with default Hoje data.
    - Quick filter controls and manual date range controls are available.
    - Central grid row shape clearly matches a duplicata/titulo to pay.
    - Selecting a row updates details/products/parcelas/rateio through the service.
    - Mutating buttons remain disabled and non-operational.
    - View contains no SQL or JDBC usage.
  </acceptance_criteria>
  <done>The user can inspect Contas a Pagar duplicatas in Vaadin without write capability.</done>
</task>

<task type="auto">
  <name>Task 4: Add tests and phase guardrails</name>
  <files>src/test/java/br/com/salome/core/application/contaspagar/**, docs/setup/salome-core-local.md, .planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-01-SUMMARY.md</files>
  <read_first>.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md, .planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-RESEARCH.md, src/test/java/br/com/salome/core/application/contaspagar/GestaoPagamentosServiceTest.java</read_first>
  <action>Add or update focused tests for the Phase 7 read-only behavior: default filter is Hoje, service propagates the filter to the repository, selected duplicata detail is resolved by nota id + duplicata id, and the repository contract exposes read methods only. Keep tests independent of a live MySQL database. Update setup documentation only if execution discovers a new local run or verification note for Phase 7. After implementation and verification, create `07-01-SUMMARY.md` documenting delivered files, origins preserved, tests run, and any tooling limitations.</action>
  <verify>mvn test; rg -n "GestaoPagamentosFiltro|hoje|read-only|readOnly|GestaoPagamentosService" src/test/java/br/com/salome/core/application/contaspagar docs/setup/salome-core-local.md</verify>
  <acceptance_criteria>
    - Unit tests cover default Hoje filtering and service filter propagation.
    - Repository contract remains read-only.
    - Tests do not require legacy DB credentials.
    - Summary documents rule/query origins and confirms no legacy code was changed.
  </acceptance_criteria>
  <done>Phase 7 behavior is covered by focused tests and documented for review.</done>
</task>

<task type="auto">
  <name>Task 5: Final verification and safety checks</name>
  <files>.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-01-SUMMARY.md</files>
  <read_first>AGENTS.md, .planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-CONTEXT.md</read_first>
  <action>Run final verification commands. Confirm tests pass where local Java/Maven tooling allows. Confirm `ui` has no SQL/JDBC, Phase 7 infrastructure has no mutating SQL/API usage, Flyway remains disabled for the legacy datasource, and no tracked `salome-legacy` files changed. Record any inability to run Maven due to missing JDK 25/Maven in `07-01-SUMMARY.md` rather than pretending verification passed.</action>
  <verify>mvn test; if (rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/ui) { exit 1 }; if (rg -n "INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar) { exit 1 }; rg -n "flyway:\\s*\\r?\\n\\s*enabled:\\s*false|enabled: false" src/main/resources/application.yml docs/setup/salome-core-local.md; git status --short --untracked-files=no salome-legacy</verify>
  <acceptance_criteria>
    - `mvn test` passes or the exact local tooling blocker is documented.
    - UI layer has no SQL/JdbcTemplate usage.
    - Legacy Contas a Pagar adapter has no mutating operations.
    - Flyway remains disabled/safe for the legacy datasource.
    - No tracked file under `salome-legacy` is modified.
  </acceptance_criteria>
  <done>Phase 7 is ready for execution review with safety guardrails intact.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test` passes with JDK 25 and without legacy DB credentials, or the exact tooling blocker is documented in the summary.
- [ ] `rg -n "Hoje|Vencidas|Proximos|Todas|DatePicker" src/main/java/br/com/salome/core` confirms quick/manual filters exist.
- [ ] `rg -n "FROM notacompraduplicatas|idNotaCompraDuplicatas|datapagamento|vencimento|CURRENT_DATE" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar` confirms duplicata-first legacy reads.
- [ ] `rg -n "SELECT|JdbcTemplate|INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/ui` returns no matches.
- [ ] `rg -n "INSERT|UPDATE|DELETE|executeUpdate" src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar` returns no matches.
- [ ] `rg -n "spring.flyway.enabled: false|enabled: false" src/main/resources/application.yml docs/setup/salome-core-local.md` confirms Flyway safety remains visible.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked modifications.
</verification>

<success_criteria>
- User can open the Vaadin Gestao de Pagamentos / Contas a Pagar read-only screen.
- The initial list shows only duplicatas vencendo hoje by default.
- Vencidas, Proximos dias, Todas and manual period filtering are available.
- The central grid is a duplicata queue with fornecedor, documento/nota, parcela, vencimento, valor, status and filial.
- Data is loaded through `GestaoPagamentosService` and `GestaoPagamentosRepository`/legacy adapter.
- Products, parcel/payment read fields and plano/centro de contas information appear in the selected detail area.
- Central de Pagamentos is visible but blocked for baixa, cheque, confirmacao, exclusao and any write.
- No SQL or heavy financial rule is placed in the Vaadin View.
- No legacy code or production schema is changed.
- Phase requirements READ-01 and READ-02 are covered.
</success_criteria>

<output>
After completion, create `.planning/phases/07-criar-tela-contas-a-pagar-somente-leitura/07-01-SUMMARY.md`.
</output>
