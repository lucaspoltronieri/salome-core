---
phase: 05-propor-arquitetura-do-salome-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/salome-core-architecture.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "Decision D-01 is covered: legacy identity remains the source of truth while Spring Security centralizes authentication and authorization."
    - "Decision D-02 is covered: a legacy adapter maps Conecta.getUsuario(), UsuarioController, usuario and usuarioalerta semantics into Principal and authorities."
    - "Decision D-03 is covered: top-level layers are ui, application, domain, infrastructure and security, with feature subpackages inside each layer."
    - "Decision D-04 is covered: infrastructure repositories/adapters use JDBC/JdbcTemplate over legacy MySQL."
    - "Decision D-05 is covered: repositories may be CRUD-capable, but the first delivery remains read-only."
    - "Decision D-06 is covered: the architecture supports future CRUD while preserving the roadmap order of architecture, validated read-only web, then mutations."
    - "A revisable architecture proposal for salome-core exists before project creation."
    - "The proposal separates ui, application, domain, infrastructure and security responsibilities."
    - "The proposal states that Vaadin Views call Services and do not contain SQL or heavy financial rules."
    - "The proposal states that Services call Repositories/Adapters for legacy database access."
    - "The proposal preserves read-only first for the first Vaadin slice while allowing future CRUD through command services."
    - "The proposal documents Spring Security integration with legacy user semantics through adapters."
  artifacts:
    - path: "docs/architecture/salome-core-architecture.md"
      provides: "Architecture proposal for Phase 5"
      min_lines: 80
      contains: "salome-core"
  key_links:
    - from: "docs/architecture/salome-core-architecture.md"
      to: ".planning/phases/05-propor-arquitetura-do-salome-core/05-CONTEXT.md"
      via: "canonical reference section"
      pattern: "05-CONTEXT.md"
    - from: "docs/architecture/salome-core-architecture.md"
      to: ".planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md"
      via: "legacy mapping references"
      pattern: "CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md"
---

<objective>
Create the Phase 5 architecture proposal for `salome-core`.

Purpose: turn the locked discussion decisions and legacy maps into a concrete, reviewable architecture contract before Phase 6 creates the Java 25 + Spring Boot 4 + Vaadin project.
Output: `docs/architecture/salome-core-architecture.md`, documenting layer responsibilities, package layout, dependency direction, read/write boundaries, legacy database adapters, Spring Security integration, Flyway governance and testing expectations.

Decision coverage: this plan translates D-01, D-02, D-03, D-04, D-05 and D-06 from `05-CONTEXT.md` into the architecture proposal.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-propor-arquitetura-do-salome-core/05-CONTEXT.md
@.planning/phases/05-propor-arquitetura-do-salome-core/05-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/USUARIO-ACESSO-MAPA.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the architecture proposal skeleton</name>
  <files>docs/architecture/salome-core-architecture.md</files>
  <read_first>AGENTS.md, .planning/phases/05-propor-arquitetura-do-salome-core/05-CONTEXT.md, .planning/phases/05-propor-arquitetura-do-salome-core/05-RESEARCH.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Create `docs/architecture/salome-core-architecture.md`. The document must be in Portuguese, ASCII-compatible, and titled `# Arquitetura alvo do salome-core`. Include these sections with exact headings: `## Objetivo`, `## Escopo e nao escopo`, `## Camadas e responsabilidades`, `## Estrutura de pacotes proposta`, `## Fluxo de dependencia`, `## Politica read-first e escrita futura`, `## Integracao com banco legado`, `## Seguranca e usuario logado`, `## Governanca de schema e Flyway`, `## Regras financeiras e testes`, `## Referencias canonicas`, and `## Checklist de revisao`. Keep the phase scoped to architecture documentation only: do not create Maven, Java, Vaadin, Flyway migration, or legacy code files.</action>
  <verify>Test-Path docs/architecture/salome-core-architecture.md</verify>
  <acceptance_criteria>
    - `docs/architecture/salome-core-architecture.md` exists.
    - The file contains `# Arquitetura alvo do salome-core`.
    - The file contains all required section headings listed in the action.
    - No `pom.xml`, `src/main/java`, `src/main/resources/db/migration`, or files under `salome-legacy` are created or modified by this plan.
  </acceptance_criteria>
  <done>The proposal skeleton exists and is scoped to documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Document layer contracts and legacy boundaries</name>
  <files>docs/architecture/salome-core-architecture.md</files>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md, .planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md</read_first>
  <action>Fill the proposal with concrete contracts. In `Camadas e responsabilidades`, define `ui`, `application`, `domain`, `infrastructure` and `security`. In `Estrutura de pacotes proposta`, use top-level packages `ui`, `application`, `domain`, `infrastructure`, `security` and feature subpackages such as `contaspagar`, `notacompra`, `financeiro`, `cadastros` and `legacy`. In `Fluxo de dependencia`, state that Vaadin Views call Services, Services call Repositories/Adapters, and Repositories/Adapters access MySQL. In `Integracao com banco legado`, name the first adapter families: `LegacyContasPagarRepository`, `LegacyNotaCompraRepository`, `LegacyNotaCompraDuplicataRepository`, `LegacyExtratoRepository`, `LegacyFornecedorRepository`, `LegacyFilialRepository`, `LegacyBancoRepository`, `LegacyPlanoContasRepository` and `LegacyPlanoContasCentroCustoRepository`. Cite the mapped legacy origins for `ContasPagar`, `NotaCompra`, `NotaCompraDuplicatas`, `Extrato`, `Banco`, `Fornecedor`, `Filial` and `PlanoContas*`.</action>
  <verify>rg -n "Views chamam Services|Services chamam Repositories|LegacyContasPagarRepository|LegacyNotaCompraDuplicataRepository|PlanoContas" docs/architecture/salome-core-architecture.md</verify>
  <acceptance_criteria>
    - The document contains the exact phrase `Views Vaadin chamam Services`.
    - The document contains the exact phrase `Services chamam Repositories/Adapters`.
    - The document contains the exact phrase `Repositories/Adapters acessam MySQL legado`.
    - The document names all adapter families listed in the action.
    - The document cites `.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md` and `.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md`.
  </acceptance_criteria>
  <done>Layer contracts and legacy integration boundaries are explicit and traceable.</done>
</task>

<task type="auto">
  <name>Task 3: Capture security, schema and test guardrails</name>
  <files>docs/architecture/salome-core-architecture.md</files>
  <read_first>.planning/codebase/USUARIO-ACESSO-MAPA.md, .planning/PROJECT.md, AGENTS.md</read_first>
  <action>Complete the security, schema and validation sections. `Seguranca e usuario logado` must state that Spring Security centralizes authentication/authorization and a legacy adapter maps `Conecta.getUsuario()`, `UsuarioController`, `usuario` and `usuarioalerta` semantics into `Principal` and authorities. `Governanca de schema e Flyway` must state that Phase 5 creates no migrations, production DB is not changed, and any future table/field change requires a versioned SQL script. `Regras financeiras e testes` must list the critical future rule families that need tests before migration: baixa, exclusao, edicao, rateio, fornecedor, produto, plano de contas, filial and usuario logado. `Checklist de revisao` must map ARCH-01 through ARCH-04 to concrete review checks.</action>
  <verify>rg -n "Spring Security|Conecta.getUsuario\\(\\)|usuarioalerta|Flyway|ARCH-01|ARCH-04|baixa|rateio" docs/architecture/salome-core-architecture.md</verify>
  <acceptance_criteria>
    - The document states Spring Security centralizes authentication and authorization.
    - The document mentions `Conecta.getUsuario()`, `UsuarioController`, `usuario` and `usuarioalerta`.
    - The document states no production database change happens in Phase 5.
    - The document states future schema changes require versioned SQL scripts.
    - The checklist includes ARCH-01, ARCH-02, ARCH-03 and ARCH-04.
  </acceptance_criteria>
  <done>Security, schema and testing guardrails are documented for review.</done>
</task>

</tasks>

<verification>
- [ ] `Test-Path docs/architecture/salome-core-architecture.md` returns true.
- [ ] `rg -n "Views Vaadin chamam Services|Services chamam Repositories/Adapters|Repositories/Adapters acessam MySQL legado" docs/architecture/salome-core-architecture.md` finds all dependency-direction phrases.
- [ ] `rg -n "ARCH-01|ARCH-02|ARCH-03|ARCH-04" docs/architecture/salome-core-architecture.md` confirms all architecture requirements are reviewable.
- [ ] `git status --short salome-legacy` shows no modifications under the legacy code.
</verification>

<success_criteria>
- `docs/architecture/salome-core-architecture.md` exists and is substantive.
- The proposal covers ARCH-01, ARCH-02, ARCH-03 and ARCH-04.
- The proposal preserves the read-only first strategy and future CRUD boundary.
- The proposal documents Spring Security plus legacy user adapter integration.
- The proposal cites canonical planning and codebase mapping references.
- No code or database migration is created in Phase 5.
</success_criteria>

<output>
After completion, create `.planning/phases/05-propor-arquitetura-do-salome-core/05-01-SUMMARY.md`
</output>
