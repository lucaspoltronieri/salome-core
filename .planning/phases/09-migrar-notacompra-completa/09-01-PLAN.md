---
phase: 09-migrar-notacompra-completa
plan: 01
type: execute
wave: 1
depends_on:
  - phase-08
files_modified:
  - src/main/java/br/com/salome/core/domain/notacompra/**
  - src/main/java/br/com/salome/core/application/notacompra/**
  - src/main/java/br/com/salome/core/infrastructure/legacy/notacompra/**
  - src/main/java/br/com/salome/core/security/**
  - src/test/java/br/com/salome/core/domain/notacompra/**
  - src/test/java/br/com/salome/core/application/notacompra/**
autonomous: true
requirements:
  - FULL-01
  - FULL-02
  - FULL-03
  - FULL-04
  - FULL-06
  - FULL-07
user_setup:
  - "Use JDK 25 and Maven when running tests."
  - "Do not point write-capable legacy DB configuration at production."
must_haves:
  truths:
    - "Decision D-01 is covered: the foundation models the complete NotaCompra scope, not a partial write slice."
    - "Decision D-02 is covered: every rule type created in this plan stores a legacy origin string with class, method/button, DAO/query and table when known."
    - "Decision D-08 is covered: lock status detects duplicata paid or linked to Extrato."
    - "Decision D-09 is covered: XML-after-payment is represented as a distinct permission-gated exception, not as normal editing."
    - "Decision D-10 is covered: note delete preconditions must check duplicatas before any delete repository call."
    - "Decision D-11 is covered: validated financial rules initially match mapped legacy behavior only."
    - "Decision D-12 is covered: write permission uses legacy-equivalent current user context where available and provisional finance operation role where not mapped."
    - "No SQL or financial rule is added to a Vaadin View."
    - "No file under salome-legacy is modified."
  artifacts:
    - path: "src/main/java/br/com/salome/core/domain/notacompra/NotaCompraLockStatus.java"
      provides: "Financial lock reason derived from duplicata baixa/extrato state"
      contains: "idExtrato"
    - path: "src/main/java/br/com/salome/core/application/notacompra/NotaCompraPermissionService.java"
      provides: "Write and XML exception authorization boundary"
      contains: "ROLE_FINANCEIRO_OPERACAO"
    - path: "src/main/java/br/com/salome/core/infrastructure/legacy/notacompra/LegacyNotaCompraRepository.java"
      provides: "Header repository shell and duplicate-note query"
      contains: "notacompra"
  key_links:
    - from: "application.notacompra services"
      to: "infrastructure.legacy.notacompra repositories"
      via: "ports/interfaces and JdbcTemplate implementations"
      pattern: "LegacyNotaCompra"
---

<objective>
Create the Phase 9 write-safe foundation for complete `NotaCompra` migration: domain models, command/read contracts, legacy repository shells, lock detection, permission gates and tests for critical financial rules before the Vaadin editing surface is opened.

This plan is Wave 1. It prepares all later Phase 9 plans without exposing user-facing write controls yet.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-migrar-notacompra-completa/09-CONTEXT.md
@.planning/phases/09-migrar-notacompra-completa/09-RESEARCH.md
@docs/architecture/salome-core-architecture.md
@.planning/codebase/CONTAS-PAGAR-CLASSES-MAPA-TECNICO.md
@.planning/codebase/CONTAS-PAGAR-MAPA-BANCO-QUERIES.md
@.planning/codebase/USUARIO-ACESSO-MAPA.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotaCompra domain/read/command contracts</name>
  <files>src/main/java/br/com/salome/core/domain/notacompra/**, src/test/java/br/com/salome/core/domain/notacompra/**</files>
  <read_first>.planning/phases/09-migrar-notacompra-completa/09-CONTEXT.md, .planning/phases/09-migrar-notacompra-completa/09-RESEARCH.md</read_first>
  <action>Create records/classes for document list rows, header detail, product rows, duplicata rows, rateio rows, section totals, legacy origin references, lock status and validation failures. Create commands for header, product, duplicata, rateio, delete and XML link/import exception. Keep commands section-scoped so header save cannot carry product/rateio/duplicata mutations. Include an explicit `LegacyOrigin` or equivalent value that can cite class, method/button, DAO/query and table.</action>
  <verify>rg -n "record .*NotaCompra|LegacyOrigin|LockStatus|Salvar.*Command|Excluir.*Command|Xml" src/main/java/br/com/salome/core/domain/notacompra src/test/java/br/com/salome/core/domain/notacompra</verify>
  <acceptance_criteria>
    - Domain package has separate read models and commands.
    - Every command can be traced to a section: header, products, duplicatas, rateio, delete or XML.
    - Models contain enough data to render Documento Entrada and Documento Entrada Detalhes.
    - No domain type depends on Vaadin, JdbcTemplate or Spring UI classes.
  </acceptance_criteria>
  <done>NotaCompra has domain contracts ready for services and adapters.</done>
</task>

<task type="auto">
  <name>Task 2: Add repository ports and legacy adapter skeletons</name>
  <files>src/main/java/br/com/salome/core/application/notacompra/**, src/main/java/br/com/salome/core/infrastructure/legacy/notacompra/**</files>
  <read_first>docs/architecture/salome-core-architecture.md, src/main/java/br/com/salome/core/application/contaspagar/GestaoPagamentosRepository.java, src/main/java/br/com/salome/core/infrastructure/legacy/contaspagar/LegacyGestaoPagamentosRepository.java</read_first>
  <action>Create application repository ports and JdbcTemplate adapter classes for header, products, duplicatas, rateio and lookups. Implement read methods and the critical precondition queries first: duplicate invoice check, lock status by `notacompraduplicatas.datapagamento`/`idExtrato`, total rateado sum, and existence checks. Mutating methods may be declared for later plans but should throw or remain unimplemented until their owning plan wires them with tests.</action>
  <verify>rg -n "interface .*Repository|class LegacyNotaCompra|notacompra|notacompraduplicatas|notacompraprodutos|notacomprarateio|idExtrato|datapagamento" src/main/java/br/com/salome/core/application/notacompra src/main/java/br/com/salome/core/infrastructure/legacy/notacompra</verify>
  <acceptance_criteria>
    - SQL lives only in `infrastructure.legacy.notacompra`.
    - Repository ports live in `application.notacompra`.
    - Duplicate-note and lock-status queries are implemented.
    - No Vaadin View accesses any repository or SQL directly.
  </acceptance_criteria>
  <done>Legacy data access boundaries exist for Phase 9.</done>
</task>

<task type="auto">
  <name>Task 3: Implement financial rule services and permission gates</name>
  <files>src/main/java/br/com/salome/core/application/notacompra/**, src/main/java/br/com/salome/core/security/**, src/test/java/br/com/salome/core/application/notacompra/**</files>
  <read_first>.planning/codebase/USUARIO-ACESSO-MAPA.md, src/main/java/br/com/salome/core/security/CurrentUser.java, src/main/java/br/com/salome/core/security/SecurityRoles.java</read_first>
  <action>Add services for lock evaluation, permission checks and section validation. Write permission should require `ROLE_FINANCEIRO_OPERACAO` or `ROLE_ADMIN` as the provisional equivalent. XML-after-payment should be a distinct permission check and should not unlock normal editing. Validation should include duplicate note, model/chave/protocolo rules, fechamento pagar hook, duplicata vencimento before emissao, duplicata paid/extrato lock and rateio remaining-value rule.</action>
  <verify>rg -n "ROLE_FINANCEIRO_OPERACAO|ROLE_ADMIN|pode|permiss|datapagamento|idExtrato|fechamento|existeNotafiscal|valorRestante" src/main/java/br/com/salome/core/application/notacompra src/test/java/br/com/salome/core/application/notacompra</verify>
  <acceptance_criteria>
    - Unauthorized write attempts fail before repository mutation.
    - XML exception has a separate method/test.
    - Normal editing remains blocked for paid/extrato-linked notes.
    - Rule failures include legacy origin references.
  </acceptance_criteria>
  <done>Critical Phase 9 rules have testable service boundaries.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused foundation tests</name>
  <files>src/test/java/br/com/salome/core/domain/notacompra/**, src/test/java/br/com/salome/core/application/notacompra/**</files>
  <read_first>.planning/phases/09-migrar-notacompra-completa/09-RESEARCH.md</read_first>
  <action>Add database-free unit tests for duplicate-note validation, chave/protocolo validation, lock detection, delete preconditions, rateio remaining-value calculation including edit, duplicata vencimento before emissao and permission checks. Use fake repository ports and current-user fixtures.</action>
  <verify>mvn test -Dtest=*NotaCompra*Test,*Rateio*Test,*Duplicata*Test</verify>
  <acceptance_criteria>
    - Tests do not require a live MySQL database.
    - Tests prove critical rules fail closed.
    - Tests cite rule origins in expected failures where relevant.
  </acceptance_criteria>
  <done>Financial foundation is guarded before UI write work begins.</done>
</task>

</tasks>

<verification>
- [ ] `mvn test -Dtest=*NotaCompra*Test,*Rateio*Test,*Duplicata*Test` passes or exact JDK/Maven blocker is documented.
- [ ] `rg -n "SELECT|INSERT|UPDATE|DELETE|JdbcTemplate" src/main/java/br/com/salome/core/ui` returns no matches.
- [ ] `rg -n "salome-legacy" src/main/java/br/com/salome/core/domain/notacompra src/main/java/br/com/salome/core/application/notacompra` shows origins only, not direct imports/calls.
- [ ] `git status --short --untracked-files=no salome-legacy` shows no tracked legacy modifications.
</verification>

<success_criteria>
- Phase 9 has a complete `notacompra` foundation aligned with `ui -> application -> domain -> infrastructure`.
- Critical financial rules are explicit, traceable and covered by unit tests.
- Permission and lock behavior is ready for later write commands.
- No user-facing write UI is exposed by this foundation plan alone.
</success_criteria>

<output>
After completion, create `.planning/phases/09-migrar-notacompra-completa/09-01-SUMMARY.md`.
</output>
